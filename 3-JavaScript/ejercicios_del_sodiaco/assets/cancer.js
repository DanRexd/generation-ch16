/* Crea una calculadora para convertir un valor ingresado por el usuario a grados Celsius, Fahrenheit y Kelvin.

Considera:

Solicitar el valor por medio de un prompt
Crear funciones para realizar los cálculos
Mostrar los resultados mediante alertas
 */

/* 
function ctof (grados){
    return (grados * (9 / 5) + 32);
}
function ctok (grados){
    return (grados + 273.15);
}

function ftoc (grados){
    return ((grados - 32) * 5/9 );
}
function ftok (grados){
    return ((grados - 32) * 5/9 + 273);
}

function ktoc (grados){
    return (grados - 273.15);
}1
function ktof (grados){
    return ((grados - 273.15)* 9/5 + 32);
}

let grados = (Number (prompt ("Incierte grados")));
let unidades = ((prompt("¿En que unidades esta? \n - C para Celsious \n - F para Fahreinheit \n - K para Kelvin")));

if (unidades === "C"){
    alert (grados + " grados Celsious equivale a " + ctof(grados) + " grados Fahreinheit y a " + ctok(grados) + " grados Kelvin ");
}
else if (unidades === "F"){
    alert (grados + " grados Fahreinheit equivalen a " + ftoc(grados) + " grados Celsious y a " + ftok(grados) + " grados Kelvin ");
}
else if (unidades === "K"){
    alert (grados + " grados Kelvin equivalen a " + ktoc(grados) + " grados celsious y a " + ktof(grados) + " grados fahreinheit ");
}
else {1
    alert("Opcion no valida")
} */

let y;
console.log(y === null);
